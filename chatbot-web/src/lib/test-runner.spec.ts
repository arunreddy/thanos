import './test-runner';

describe('Test Runner', () => {
  it('should execute all utils functions', () => {
    // This just imports the test-runner module
    // The actual tests are in utils.spec.ts
    expect(true).toBe(true);
  });
});